name: Build Qt Project

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '6.9.3'
        target: 'desktop'
        arch: 'win64_mingw'
        
    - name: Build with qmake
      run: |
        echo "🏗️ Building with qmake..."
        # Используем полные пути как в логе
        /d/a/CAS-Colloquium-Project/Qt/6.9.3/mingw_64/bin/qmake
        /d/a/CAS-Colloquium-Project/Qt/6.9.3/mingw_64/bin/mingw32-make
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ComputerAlgebraSystem
        path: build/ComputerAlgebraSystem.exe